# 初痕轻量依赖 — 不含 torch / transformers 等重型包
# 适用场景：仅需 MCP 服务 + 记忆检索，不需要本地 embedding 模型
#
# 安装: pip install -r requirements-lite.txt
# 注意: 轻量版需要外部 embedding 服务（如远程 Ollama、OpenAI API 等）

fastapi>=0.104.0
uvicorn[standard]>=0.24.0
chromadb>=0.4.22
httpx>=0.25.0
python-multipart>=0.0.6
python-dotenv>=1.0.0
pytest>=8.0.0
pytest-asyncio>=0.23.0
numpy>=1.24.0
rich>=13.0.0
psutil>=5.9.0
pygments>=2.15.0
