# 为什么 LongMemEval 和 LoCoMo 不该被当作记忆系统评测标准

> 本文是对 LongMemEval 和 LoCoMo 作为"AI 记忆系统 benchmark"的根本性质疑。  
> 初痕仍跑了这些测试（92% LongMemEval 纠正后），但我们拒绝把分数当作品质证明。

---

## LongMemEval

**仓库：** `xiaowu0162/LongMemEval`（ICLR 2025）

### 官方脚本总共四个文件

- `evaluate_qa.py` — 调 GPT-4o 给你的 hypothesis 打分
- `print_qa_metrics.py` — 把打分结果汇总成一张表
- `run_generation.sh` — 把全部对话 session 一次性塞进 LLM 的上下文窗口，让 LLM 直接读全文回答问题
- `run_retrieval.sh` — 用 BM25 / Contriever / Stella 从对话中搜相关片段，喂给 LLM

### 根本问题

官方没有一行代码告诉你：
- "怎么把对话注入记忆系统"
- "怎么让记忆系统随时间推移自主巩固"
- "怎么测试三个月后还记得多少"

因为人家的设计假设就是：**把数据塞进 LLM 上下文，或者用一个检索器搜出来。没有记忆什么事。**

### 具体缺陷

1. **错题**：标准答案跟对话数据对不上。不是"可能有错"，是实打实的有。
2. **LLM-as-Judge 误判**：大量正确回答被 GPT-4o 判错，仅仅因为表述方式不同。
3. **底座模型混杂**：换更强的底座模型，分数原地起飞——跟记忆系统没有关系。
4. **测的是阅读理解，不是记忆**：看起来考的是记忆，实际上就是把原文塞回去让 LLM 做推理。没有持久化存储、没有时间流逝、没有检索管线、没有认知过滤。

---

## LoCoMo

**仓库：** `snap-research/LoCoMo`（ACL 2024）

### 官方评测方式

- `evaluate_gpts.sh` — 把整段 300+ turns 的对话全部塞进 GPT 上下文
- `evaluate_rag_gpts.sh` — 用 RAG 的方式跑，但 **session_summary 和 observation 是数据集自带的，已经预处理好了**。检索那一步别人替你做完打包好了，你拿过来直接用
- `generate_session_summaries.sh` / `generate_observations.sh` — 生成上述预处理数据，但这是给需要重新生成的人用的

### 根本问题

两个官方仓库的"评测方式"本质完全一样：**数据 → 塞进 LLM 的上下文窗口 → LLM 读 → LLM 答 → GPT-4o 打分。** 全过程没有一个环节涉及"记忆"——没有持久化存储、没有时间流逝、没有检索管线、没有认知过滤。测的是 LLM 的阅读理解能力，不是任何记忆系统的记忆能力。

---

## 这对记忆系统意味着什么

你拿这两个 benchmark 去测任何记忆系统，本质上是在测"你的系统能不能把对话原文原封不动地还给 LLM"。**做得越多——摘要、情绪分析、实体提取、认知分层——反而越吃亏**，因为这些认知加工在这个测试里都是噪音。

如果靠这些分数来选型，我只能祝你幸福。

---

## 关于刷分

连题目都是错的，judge 都有大量误判，却有人跑出 90%+。是特调了 prompt？换了更强的底座模型？还是干脆上下文全塞、记忆系统根本没参与？

刷分谁都会。我可以在极短时间内把两个测试刷到 95%。但我不会告诉你是用什么方法刷的——因为那些方法跟"记忆"没有任何关系。

---

## 我们的立场

初痕是认知记忆引擎。它在对话中自然积累认知，理解人格和情绪变化，在后台自主巩固、蒸馏、发现模式，在合适的时机主动开口。**这些东西没有 benchmark 能测。**

我们宁愿做一个测不出分数但真的在"记住你"的系统，也不愿为了一把歪尺子改设计。

直到有真正评测"记忆"的 benchmark 出现——测的不是"读原文→回答问题"，而是"跨时间的认知沉淀、自主巩固、情绪理解、遗忘与权重演化"——这些分数都是噪音。

---

*作者：初痕团队 · 2026-06*
